/**
 * =============================================================================
 * EmailJS — YOUR CREDENTIALS GO HERE ONLY
 * =============================================================================
 * 1. Sign up at https://www.emailjs.com/
 * 2. Add an **Email Service** (e.g. Gmail) → copy **Service ID**
 * 3. Create an **Email Template** with these variable names (must match form):
 *      {{name}}    ← visitor’s name
 *      {{email}}   ← visitor’s email
 *      {{message}} ← visitor’s message
 *    Copy **Template ID**
 * 4. Account → **API keys** → copy **Public Key**
 *
 * Paste the three values below (replace the placeholder text, keep the quotes).
 * =============================================================================
 */
window.EMAILJS_CONFIG = {
  /** EmailJS → Account → API keys → Public Key */
  publicKey: "YOUR_PUBLIC_KEY_HERE",

  /** EmailJS → Email Services → your service → Service ID */
  serviceId: "YOUR_SERVICE_ID_HERE",

  /** EmailJS → Email Templates → your template → Template ID */
  templateId: "YOUR_TEMPLATE_ID_HERE",
};
